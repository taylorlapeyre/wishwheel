(ns wishwheel.test-data
  (:require [wishwheel.models.user :as usermodel]))


(def user
  {:first_name "taylor"
   :last_name "lapeyre"
   :email "taylor@lapeyre.com"
   :password "password"})

(def wheel
  {:name "get "})